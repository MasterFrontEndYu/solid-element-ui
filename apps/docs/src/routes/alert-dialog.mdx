## Alert Dialog 警告对话框

import { AlertDialog,Button } from "solid-element-ui";



<div class="p-4 bg-white not-prose dark:bg-zinc-950 border rounded-lg space-x-2 space-y-2">
    <AlertDialog
        title="确认删除？"
        description="此操作不可逆。"
        trigger={<Button >点击弹出</Button>}
    />


    <AlertDialog
    title="确认删除？"
    description="此操作不可逆。"
    trigger={<Button color="warning">自定义action</Button>}
    action={
        <Button color="danger">
            确定
        </Button>
    }
/>
</div>

### 导入

```tsx
import { AlertDialog,Button } from "solid-element-ui";
```

### 基础用法

```tsx
<div class="p-4 bg-white dark:bg-zinc-950 border rounded-lg space-x-2 space-y-2">
    <AlertDialog
        title="确认删除？"
        description="此操作不可逆。"
        trigger={<Button >点击弹出</Button>}
    />


    <AlertDialog
        title="确认删除？"
        description="此操作不可逆。"
        trigger={<Button color="warning">自定义action</Button>}
        action={
            <Button onclick={()=>alert(111)} color="danger">
                确定
            </Button>
        }
    />
</div>
```

### Tailwind v4 动画

```css
@import "tailwindcss";

@theme {
    @keyframes fade-in {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }
}

[data-kb-overlay] {
    animation: fade-in 0.2s;
}
[data-kb-content] {
    animation: fade-in 0.2s;
}
```
